## inventory-domain:zone

Usage:
  zone
  zone (-h | --help)

Options:
  -h --help     Show this screen.

Required Environment variables (can be defined in the .env file):
  * INFRA_DOMAIN  base domain name
